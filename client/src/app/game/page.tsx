import GameContainer from "@/components/layout/game/game";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Aposte e Diverta-se | Mines",
  description: "...",
};

export default function Game() {
  return <GameContainer />;
}
